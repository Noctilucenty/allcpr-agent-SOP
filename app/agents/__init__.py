"""Agent packages built on top of the maps-scraper-intel data engine.

Currently hosts the AutoCPR Site Management Specialist agent
(``autocpr_site_manager``). Agents here are deterministic, dependency-light,
and never call paid APIs or print secrets.
"""
